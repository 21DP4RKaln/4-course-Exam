'use client'

import { usePathname } from 'next/navigation'
import PeripheralPage from '@/app/components/Peripheral/Peripheral'

export default function PeripheralsRoute() {
  return <PeripheralPage />
}